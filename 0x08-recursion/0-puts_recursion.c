#include "main.h"
/**
     *puts_recursion – prints a string followed by a new line.
     *@s : pointer to string.
     *
     *Return : void.
     */
Void _puts_recursion(char *s)
{
            If (*s == ‘\0') /* base case */
          {
                        _putchar( ‘ \n’ )
                          return ;
           }
           _putchar( *s ) ; 
           _puts_recursion(s + 1) :             
  } 

